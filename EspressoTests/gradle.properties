org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true

# Disable configuration cache to avoid caching signing config issues
# The configuration cache can cause problems with dynamic property resolution
# and file checks during the configuration phase
org.gradle.configuration-cache=false
org.gradle.caching=true
